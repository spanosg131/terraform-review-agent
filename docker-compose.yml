services:
  agent:
    build: .
    image: terraform-review-agent:dev
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./src:/app/src
      - ./scripts:/app/scripts
    working_dir: /app
